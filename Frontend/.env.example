# Leave VITE_API_URL empty when using the Vite dev proxy (recommended for local dev)
# Set it to your production backend URL when deploying
VITE_API_URL=

# Google OAuth Client ID — same value as GOOGLE_CLIENT_ID in the backend .env
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
